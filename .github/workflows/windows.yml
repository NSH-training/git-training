name: windows workflow

run-name: windows runner

on: push
jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: printing massge
        shell: bash
        run: echo "window worflow successfull"
