name: Trigger workflow

run-name: Trigger

on: pull_request
jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: printing massge
        shell: bash
        run: echo "window worflow successfull"
